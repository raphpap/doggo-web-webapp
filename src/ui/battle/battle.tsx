// Vendor
import React from 'react';
import styled from 'react-emotion';

const Message = styled.p`
  color: rgba(255, 255, 255, 0.8);
`;

export const Battle: React.SFC = () => <Message>Battle</Message>;

export default Battle;
